#!/bin/bash

# Ensure terraform is initialized
terraform init

# Output terraform state in JSON and process it using jq to generate CSV
terraform show -json | jq -r '.values.root_module.resources[] | [.address, .type] | @csv' > output.csv

echo "Resources exported to output.csv"
