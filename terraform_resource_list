#!/bin/bash

# File to store the CSV output
output_file="resources.csv"

# Write CSV headers
echo "Resource Name,Resource Type" > $output_file

# Extract resources from Terraform state
terraform state list -json | jq -r '.[] | "\(.address),\(.type)"' >> $output_file

echo "Resources exported to $output_file"
